import { Text, View } from 'react-native';
import { globalStyle } from '../../../config/theme/globalStyle';

export const MapsScreen = () => {
    return (
        <View style={globalStyle.container}>
            <Text style={globalStyle.text}>MapsScreen</Text>
        </View>
    );
};